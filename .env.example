# Multi-device sync is entirely optional. Leave these unset and the sync
# panel simply does not appear — the app stays 100% local, as it is by
# default. See README.md "Synchronisation entre appareils" for setup.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
